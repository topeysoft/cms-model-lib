{
  "name": "cms-model-lib",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "scripts": {
    "build": "grunt build",
    "build:w": "tsc -w",
    "deploy": "npm version patch --force &&  npm run git-publish && npm publish --access=public",
    "git-add": "git add -A .",
    "git-commit": "git commit -a -m \"code update\"",
    "git-push": "git push origin master --force  && git checkout master",
    "git-publish": "npm run git-commit && npm run git-push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-ts": "^6.0.0-beta.16",
    "typescript": "^2.4.2"
  }
}
